{
  "name": "real-estate-fractionalization-dapp",
  "version": "1.0.0",
  "description": "A decentralized application for real estate fractionalization",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "compile": "cd contracts && npx hardhat compile",
    "test": "cd contracts && npx hardhat test",
    "deploy": "cd contracts && npx hardhat run scripts/deploy.js"
  },
  "keywords": ["blockchain", "ethereum", "real-estate", "fractionalization", "dapp"],
  "author": "Real Estate Fractionalization Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
